- link:
   ~/.ssh/config.d/:
    glob: true
    path: tools/ssh/config.d/*
    relink: true
    create: true
- shell:
  - command: '/bin/env bash tools/ssh/add_include.sh'
    stdout: true
    quiet: false
    description: Add include statement to ssh config
